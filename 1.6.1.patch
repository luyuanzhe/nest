diff --git a/.circleci/config.yml b/.circleci/config.yml
index 0e5dc94fb..705bc7c4b 100644
--- a/.circleci/config.yml
+++ b/.circleci/config.yml
@@ -20,10 +20,10 @@ parameters:
 aliases:
   - &restore-cache
     restore_cache:
-      key: dependency-cache-{{ checksum "package.json" }}
+      key: dependency-cache-v2-{{ checksum "package-lock.json" }}
   - &save-cache
     save_cache:
-      key: dependency-cache-{{ checksum "package.json" }}
+      key: dependency-cache-v2-{{ checksum "package-lock.json" }}
       paths:
         - ./node_modules
   - &install-deps
@@ -37,7 +37,9 @@ aliases:
   - &run-unit-tests
     run:
       name: Test
-      command: npm run test
+      command: |
+        circleci tests glob "packages/**/*.spec.ts" \
+          | circleci tests run --command="xargs node --loader ts-node/esm ./node_modules/mocha/bin/mocha.js" --split-by=timings
 
 jobs:
   build:
@@ -54,11 +56,12 @@ jobs:
       - *save-cache
       - *build-packages
 
-  test:
+  test_units:
     parameters:
       node-version:
         type: string
     working_directory: ~/nest
+    parallelism: 4
     docker:
       - image: cimg/node:<< parameters.node-version >>
     steps:
@@ -90,33 +93,24 @@ jobs:
             - checkout
             - *restore-cache
             - *install-deps
-            - *build-packages
-            - when:
-                condition:
-                  equal:
-                    [
-                      '<< parameters.node-version >>',
-                      '<< pipeline.parameters.maintenance-node-version >>',
-                    ]
-                steps:
-                  - run:
-                      name: Test (coverage)
-                      command: npm run test:cov
-                  - run:
-                      name: Collect coverage
-                      command: npm run coverage
-                  - store_artifacts:
-                      path: coverage
-            - when:
-                condition:
-                  not:
-                    equal:
-                      [
-                        '<< parameters.node-version >>',
-                        '<< pipeline.parameters.maintenance-node-version >>',
-                      ]
-                steps:
-                  - *run-unit-tests
+            - *run-unit-tests
+
+  test_coverage:
+    working_directory: ~/nest
+    docker:
+      - image: cimg/node:<< pipeline.parameters.maintenance-node-version >>
+    steps:
+      - checkout
+      - *restore-cache
+      - *install-deps
+      - run:
+          name: Test (coverage)
+          command: npm run test:cov
+      - run:
+          name: Collect coverage
+          command: npm run coverage
+      - store_artifacts:
+          path: coverage
 
   lint:
     working_directory: ~/nest
@@ -186,7 +180,7 @@ workflows:
   build-and-test:
     jobs:
       - build
-      - test:
+      - test_units:
           requires:
             - build
           matrix:
@@ -194,10 +188,12 @@ workflows:
               node-version:
                 [
                   '<< pipeline.parameters.legacy-node-version >>',
-                  '<< pipeline.parameters.maintenance-node-version >>',
                   '<< pipeline.parameters.active-node-version >>',
                   '<< pipeline.parameters.current-node-version >>',
                 ]
+      - test_coverage:
+          requires:
+            - build
       - lint:
           requires:
             - build
diff --git a/gulpfile.js b/gulpfile.js
index a0962e1a3..19394eb7c 100644
--- a/gulpfile.js
+++ b/gulpfile.js
@@ -8,9 +8,16 @@ const path = require('path');
 
 const projectDir = __dirname;
 const tsconfigPath = path.join(projectDir, 'tools/gulp/tsconfig.json');
+const defaultConcurrency = process.env.LERNA_CONCURRENCY || '5';
+
+process.env.LERNA_CONCURRENCY = defaultConcurrency;
+process.env.NX_PARALLEL = process.env.NX_PARALLEL || defaultConcurrency;
+process.env.NX_CACHE_DIRECTORY =
+  process.env.NX_CACHE_DIRECTORY || path.join(projectDir, 'node_modules/.cache/nx');
 
 require('ts-node').register({
-  project: tsconfigPath
+  project: tsconfigPath,
+  transpileOnly: true
 });
 
 require('./tools/gulp/gulpfile');
\ No newline at end of file
diff --git a/lerna.json b/lerna.json
index 0ce7ffcbf..fdb6f4a89 100644
--- a/lerna.json
+++ b/lerna.json
@@ -1,5 +1,16 @@
 {
   "packages": ["packages/*"],
+  "npmClient": "npm",
   "version": "11.1.24",
-  "$schema": "node_modules/lerna/schemas/lerna-schema.json"
+  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
+  "command": {
+    "exec": {
+      "concurrency": 5,
+      "stream": true
+    },
+    "run": {
+      "concurrency": 5,
+      "stream": true
+    }
+  }
 }
diff --git a/package.json b/package.json
index beec06dd6..d4d6bc765 100644
--- a/package.json
+++ b/package.json
@@ -14,11 +14,15 @@
   "license": "MIT",
   "author": "Kamil Mysliwiec",
   "scripts": {
-    "build": "tsc -b -v packages",
+    "build": "npm run build:packages",
     "postbuild": "npm run move:node_modules",
     "build:dev": "tsc -b -v packages --watch",
+    "build:packages": "npm run build:packages:base && npm run build:packages:parallel && npm run build:packages:testing",
+    "build:packages:base": "tsc -b -v packages/common packages/core packages/websockets",
+    "build:packages:parallel": "lerna exec --concurrency ${LERNA_CONCURRENCY:-5} --scope @nestjs/microservices --scope @nestjs/platform-express --scope @nestjs/platform-fastify --scope @nestjs/platform-socket.io --scope @nestjs/platform-ws -- tsc -b -v tsconfig.build.json",
+    "build:packages:testing": "tsc -b -v packages/testing",
     "prebuild:prod": "npm run clean",
-    "build:prod": "tsc -b -v packages",
+    "build:prod": "npm run build:packages",
     "changelog": "lerna-changelog",
     "clean": "tsc -b --clean packages",
     "move:samples": "gulp move:samples",
